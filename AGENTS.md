# Agent guidance

Preserve PostgreSQL as the source of truth. Put workflow rules in domain services, enforce authorization at every server boundary, and retain the native allocation and booking constraints. Run focused tests and strict type checking after changes. Never edit an applied migration; add a new migration.
