# Legacy PostgreSQL migrations

These migrations were the original PostgreSQL baseline. They are retained for historical reference after the project switched to SQLite and are deliberately outside Prisma's active migration directory. Do not run them against the SQLite schema.
